name: fednow-cam29-skeleton
rail: fednow
version: 0.1
lifecycle:
  plugin: placeholder-lifecycle
steps:
  - id: step-001
    name: placeholder-transport
    type: transport
    plugin: placeholder-transport
    input:
      message: "replace with CAMT.029 request payload"
evidence:
  format: json
