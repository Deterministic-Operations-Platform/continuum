name: fednow-cam29-success
rail: fednow

lifecycle:
  start:
    - applauncher

steps:
  - send:
      via: transport-postman
      requestRef: cam29-valid
  - verify:
      via: verify-mongo
      collection: inbound_requests
      expect:
        addMeToMessage: true

evidence:
  export: audit-bundle
